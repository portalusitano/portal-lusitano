"use client";

export default function CupoesContent() {
  return (
    <div className="p-6">
      <h1 className="text-3xl font-bold text-[var(--gold)] mb-6">💰 Cupões de Desconto</h1>
      <div className="bg-[var(--background-secondary)] border border-[var(--background-elevated)] rounded-lg p-6">
        <p className="text-gray-400">Gestão de cupões será carregada aqui...</p>
      </div>
    </div>
  );
}
